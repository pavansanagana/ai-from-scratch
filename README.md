# AI From Scratch → PyTorch

Neural network built from scratch in NumPy + equivalent PyTorch training pipeline.

## Setup
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

## Run demo
python demo/run_demo.py

## Run individually
python scripts/train_numpy.py
python scripts/train_torch.py
